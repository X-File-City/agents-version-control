{
  "id": "repomint-intro",
  "title": "RepoMint: GitHub for Agents",
  "description": "A guided walkthrough of the RepoMint MCP server — a Cloudflare Workers demo that lets authenticated agents create repos, run preview builds, and manage pull requests.",
  "version": "1.0.0",
  "steps": [
    {
      "id": "overview",
      "title": "Project Overview",
      "description": "RepoMint is an MCP (Model Context Protocol) server running on Cloudflare Workers. It gives AI agents tools to manage Git repositories, seed Worker starter files, run preview builds, and merge pull requests. Built on Workers, Durable Objects, D1, and KV.",
      "focusDirectory": "",
      "colorMode": "fileTypes"
    },
    {
      "id": "entry-and-config",
      "title": "Worker Entry & Configuration",
      "description": "The Worker boots from src/index.ts, with src/config.ts handling environment-driven configuration and src/oauth.ts wiring up the @cloudflare/workers-oauth-provider flow. wrangler.jsonc declares the Durable Object, D1, and KV bindings.",
      "focusDirectory": "",
      "highlightLayers": [
        {
          "id": "entry-points",
          "name": "Entry Points & Config",
          "color": "#3b82f6",
          "items": [
            { "path": "src/index.ts", "type": "file" },
            { "path": "src/config.ts", "type": "file" },
            { "path": "src/oauth.ts", "type": "file" },
            { "path": "wrangler.jsonc", "type": "file" }
          ],
          "opacity": 0.75,
          "renderStrategy": "border"
        }
      ]
    },
    {
      "id": "mcp-layer",
      "title": "MCP Agent Surface",
      "description": "src/mcp is the agent-facing API. agent.ts defines the MCP agent built on the Cloudflare Agents SDK, and handlers.ts implements the tool methods agents call to manipulate repos.",
      "focusDirectory": "src/mcp",
      "colorMode": "fileTypes"
    },
    {
      "id": "durable-objects",
      "title": "Per-Repo Durable Object",
      "description": "src/do/repo.ts is the Durable Object that owns per-repository state. Each repo gets a single coordinator instance — making git operations, build runs, and PR state safely serialized per repo.",
      "focusDirectory": "src/do",
      "highlightLayers": [
        {
          "id": "do-core",
          "name": "Repo Durable Object",
          "color": "#a855f7",
          "items": [
            { "path": "src/do/repo.ts", "type": "file" }
          ],
          "opacity": 0.8,
          "renderStrategy": "border"
        }
      ]
    },
    {
      "id": "services",
      "title": "Services Layer",
      "description": "src/services holds the operational glue: git-ops.ts wraps isomorphic-git over an in-memory FS, repo-seed.ts seeds new repos with starter files, sandbox.ts and wrangler-run.ts execute preview/deploy builds via @cloudflare/sandbox, and artifacts.ts manages build outputs.",
      "focusDirectory": "src/services",
      "colorMode": "fileTypes"
    },
    {
      "id": "data-layer",
      "title": "D1 Database Layer",
      "description": "src/db contains the control-plane persistence: schema.ts defines the D1 tables and queries.ts wraps typed access patterns. Schema changes flow through the migrations/ directory.",
      "focusDirectory": "",
      "highlightLayers": [
        {
          "id": "data",
          "name": "Database & Migrations",
          "color": "#10b981",
          "items": [
            { "path": "src/db", "type": "directory" },
            { "path": "migrations", "type": "directory" }
          ],
          "opacity": 0.75,
          "renderStrategy": "border"
        }
      ]
    },
    {
      "id": "tests",
      "title": "Tests & Tooling",
      "description": "Tests run on @cloudflare/vitest-pool-workers, which executes tests inside the Workers runtime so bindings (DOs, D1, KV) behave like production. test/index.spec.ts is the entry test file.",
      "focusDirectory": "test",
      "colorMode": "fileTypes"
    },
    {
      "id": "next-steps",
      "title": "Next Steps",
      "description": "To run locally: `npm install && npm run dev`. To deploy: `npm run deploy`. After changing bindings in wrangler.jsonc, run `npm run cf-typegen` to regenerate Worker types. See README.md for demo caveats — this is a proof-of-concept, not production-hardened.",
      "focusDirectory": "",
      "colorMode": "fileTypes"
    }
  ]
}
